const baseUrlJsonplaceholder = 'https://jsonplaceholder.typicode.com'
const baseUrlDummyjson = 'https://dummyjson.com'

export const urlsJsonplaceholder = {
    users:{
        usersUrl: `${baseUrlJsonplaceholder}/users`,
    },

    posts:{
        postsUrl: `${baseUrlJsonplaceholder}/posts`,
    },

    comments:{
        commentsUrl: `${baseUrlJsonplaceholder}/comments`
    }
}

export const urlsDummyjson = {
    users:{
        usersUrl: `${baseUrlDummyjson}/users`,
    },

    posts:{
        postsUrl: `${baseUrlDummyjson}/posts`,
    },

    comments:{
        commentsUrl: `${baseUrlDummyjson}/comments`
    }
}